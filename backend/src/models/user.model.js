'use strict';

/**
 * User Model
 * Defines the schema and model for the User entity.
 * Swap the placeholder export for your ORM / ODM model (e.g. Mongoose, Prisma).
 *
 * Expected fields:
 *   id, email, passwordHash, displayName, role, createdAt, updatedAt
 */

// TODO: define schema and export the User model

module.exports = {};
