'use strict';

/**
 * Content Model
 * Defines the schema and model for a Content item.
 * Swap the placeholder export for your ORM / ODM model (e.g. Mongoose, Prisma).
 *
 * Expected fields:
 *   id, userId, platform, prompt, generatedText, status, createdAt, updatedAt
 *
 * Supported platforms: tiktok | instagram_reels | youtube_shorts | twitter
 */

// TODO: define schema and export the Content model

module.exports = {};
