'use strict';

/**
 * Rate Limiter Middleware
 * Wraps express-rate-limit for easy route-level application.
 * Install: npm install express-rate-limit
 *
 * Usage: router.use(rateLimiter.api)
 */

// TODO: configure and export limiter instances (api, auth, strict)

module.exports = {};
