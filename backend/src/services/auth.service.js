'use strict';

/**
 * Auth Service
 * Contains business logic for authentication.
 * Depends on: User model, JWT util, bcrypt util.
 */

// TODO: implement register(data), login(data), logout(user)

module.exports = {};
