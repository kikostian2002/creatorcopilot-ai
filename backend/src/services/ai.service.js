'use strict';

/**
 * AI Service
 * Wrapper around the AI provider (e.g. OpenAI).
 * Called by content.service.js when generating content suggestions.
 */

// TODO: implement generateContent(prompt, options)

module.exports = {};
